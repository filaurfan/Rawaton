2<div class="wrap">
      <div class="section group"> 
      	 {{#if success_msg}}
            <div class="alert alert-success">{{ success_msg }}</div>
         {{/if}}

         {{#if error_msg}}
            <div class="alert alert-danger">{{error_msg}}</div>
         {{/if}}

         {{#if error}}
            <div class="alert alert-danger">{{error}}</div>
         {{/if}}
			<form class="form-horizontal" method="post" action="/users/login">
			<div class="login-form">
					<a href="/"><center><img src="/images/logo.png"></center><br><br></a>
				    <h1><center>LOGIN</h1></center>
				    <h4><center>Belum punya akun? silahkan <a href="/users/register">register</a></center></h4>

				    <div class="form-group ">
				       	<input type="text" class="form-control" placeholder="Username " id="UserName" name="username">
				       	<i class="fa fa-user"></i>
				    </div>
				    <div class="form-group log-status">
				       	<input type="password" class="form-control" placeholder="Password" id="Passwod" name="password">
				       	<i class="fa fa-lock"></i>
				    </div>
				      	<span class="alert">Invalid Credentials</span>
				      	<a class="link" href="#">Lost your password?</a>
				    <button type="submit" class="log-btn" >Log in</button>
				    </div>
			</form>
      </div>      
</div>